yfinance>=0.2.31
pandas>=2.0
numpy>=1.24
requests>=2.31
streamlit>=1.30
plotly>=5.18
# NOTE: Postgres support (psycopg2-binary) is intentionally NOT listed here.
# The app falls back to SQLite automatically when it's absent. Only add
# "psycopg2-binary>=2.9" below if/when you set up a DATABASE_URL (Supabase),
# so a failed build of this package can never take down the whole app.
